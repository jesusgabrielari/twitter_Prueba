<nav class= "navbar navbar-expand-lg navbar-dark bg-dark">
   <div class="container">
        <a class="navbar-brand ml-5" href="<%= root_path %> "><i class="fas fa-guitar" pr-2"></i>Scene  <i class="fas fa-volume-up"></i></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                        <%= link_to 'Explorar', all_tweets_path, class: 'nav-link' %> 
                </li>

                <% if user_signed_in? %>
                <li class="nav-item">
                        <a class="nav-link">@<%= current_user.username %></a>
                </li>  
                <% if current_user.admin %>  
                <li class="nav-item">
                        <%= link_to 'Administrador', admin_root_path, class: 'nav-link' %>
                </li>
                <% end %>
                <li class="nav-item">
                        <%= link_to 'Cerrar sesion', destroy_user_session_path, method: :delete, class: 'nav-link' %> 
                </li>
                <% else %>
                <li class="nav-item">
                        <%= link_to 'Registro', new_user_registration_path, class: 'nav-link' %> 
                </li>
                <li class="nav-item">
                        <%= link_to 'Ingresar', new_user_session_path, class: 'nav-link' %>
                </li>
                <% end %>
                <form class="form-group float-right my-1 my-lg-0">
                    <%= form_tag(root_path, method: :get) do %>
                <div class="form-group">
                    <%= submit_tag("Buscar", :class => "btn btn-danger text-white float-right") %>
                    <% link_to "Limpiar", root_path, :class => "btn btn-info float-right" %>
                    <%= text_field_tag(:search, params[:search], :class => "w-50 float-right form-control") %>
                </form>
                    <% end %>

    </div>
            </ul>     
        </div> 
    </div>       
</nav>             
        